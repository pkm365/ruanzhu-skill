---
name: ruanzhu-materials
description: >
  为中国软件著作权登记（软著）准备鉴别材料：申请表、操作手册、源代码前30后30页，按某软件公司 2026.8
  内部格式规范与已过审范例生成 Word。用户提到软著、软件著作权、著作权登记、申请表、操作手册、源代码鉴别材料、
  前30页后30页、版权中心时启用。基于真实项目产出草稿并在关键节点停下等人确认；禁止编造源码；
  不负责权属判断、原创性审查，不保证过审。
---

# 软著材料生成 Skill

## 目标

从真实项目产出一套可提交的鉴别材料，落在项目根目录 `软件著作权申请资料/`：

```
软件著作权申请资料/
  草稿/
    申请表信息.yaml        ← 第 1 步，人工确认
    业务理解.md            ← 第 2 步，人工确认口径
    代码文件选择.md        ← 第 3 步，collect_sources.py 生成，人工勾选
    代码文件顺序.txt       ← 第 3 步，确认后的有序清单
    操作手册.md            ← 第 4 步，人工确认
    截图/                  ← 用户提供
  正式资料/
    申请表信息.txt         ← 对照官网逐项填写，不是上传件
    <全称>-申请表.docx     ← 公司模版填好
    <全称>_操作手册.docx
    <全称>_软件代码.docx
    代码文档索引.md        ← 每页对应文件/行号，供核对
    *.pdf                  ← 最后一步转换
```

## 硬性规则（违反任何一条都会被版权中心补正）

- 代码只能来自用户项目，禁止虚构、补写、改写；第三方/开源代码不进前后 30 页。
- 软件全称、版本号、权利人在申请表、手册封面/页眉、代码页眉**三处完全一致**；版本号统一 `V1.0` 写法。
- 硬件环境、操作系统、权利人、证件号码、开发完成日期、发表状态**必须由用户提供**，不得推测。
- 申请表"主要功能" 500–1300 字，必含研发背景、系统架构、各功能模块、技术实现方式、业务应用场景；审核系统比对功能描述↔源码↔手册。
- 手册除目录 ≥20 页，每页 ≥30 行文字，每页不能全是图；截图含标题栏、不打码（客户 logo 除外）；不出现任何代码。
- 代码每页严格 50 行纯代码，首页是入口、尾页是完整收尾；>60 页取连续前 30 + 连续后 30。
- 关键节点必须停下等确认（见工作流的"停"），不得一口气写完全套正式稿。
- 详细规则见 `references/格式要求.md`，写手册前读 `references/操作手册写作规范.md`。脚本路径：本 skill 目录下 `scripts/`（下文 `<skill>`）。

## 工作流（严格按序，每个"停"都要等用户回复）

### 第 0 步：定位项目

确认项目根目录、能否读取源码。在根目录创建 `软件著作权申请资料/草稿/`。没有源码时只能做第 1、2 步和手册骨架，**不出代码文档**。

### 第 1 步：收集登记事实 → `草稿/申请表信息.yaml`

复制 `examples/设备点检系统/申请表信息.yaml` 为模板，字段说明见 `references/申请表字段说明.md`。
先跑 `collect_sources.py` 得到真实源程序量和语言清单，再向用户要齐其余字段。能从项目推断的（语言、开发工具、支撑软件）先填草稿并标注"待确认"；不能推断的（权利人、证件号、日期、硬件、发表状态）留空问用户。
"主要功能"和"技术特点"先不写，第 2 步之后写。

**停**：列出所有字段值请用户逐项确认。

### 第 2 步：业务理解 → `草稿/业务理解.md`

读 README、目录结构、路由/菜单、页面、核心模块，写清：这是什么软件、给谁用、主流程、真实存在的页面/模块清单（这就是手册章节和"主要功能"里的模块名）、一句话申请口径。
然后写申请表"主要功能"（500–1300 字，按 `申请表字段说明.md` 的五段骨架）和"技术特点"（≤100 字），回填 yaml。

**停**：确认口径、模块清单、主要功能文案。

### 第 3 步：选代码 → `草稿/代码文件选择.md` → `草稿/代码文件顺序.txt`

```bash
uv run --with python-docx python3 <skill>/scripts/collect_sources.py <项目根> -o 草稿/代码文件选择.md --json 草稿/sources.json
```
在清单基础上给出推荐顺序：第 1 个是入口文件（main/Application/App.vue/index/登录），随后是路由、核心业务模块，最后以一个完整模块的结尾收尾。需要 ≥3000 有效行才够 60 页；不足则全交。
把推荐顺序写成 `草稿/代码文件顺序.txt`（一行一个相对路径）。

**停**：请用户确认文件顺序与排除项。

### 第 4 步：写操作手册 → `草稿/操作手册.md`

按 `references/操作手册写作规范.md` 的结构和措辞写；Markdown 语法只用 `build_manual_docx.py` 支持的那几种。
一级标题 = 第 2 步确认的模块名。没有截图的地方写 `【截图预留：请插入「××页面」截图。】`，并把需要的截图列成清单给用户（编号、页面、要求含标题栏）。
范例见 `examples/设备点检系统/操作手册.md`；如本机存在 `private/` 目录，其中的已过审真实范例优先参考。

**停**：请用户确认手册内容并提供截图到 `草稿/截图/`；截图到位后替换占位。

### 第 5 步：生成正式稿

```bash
S=<skill>/scripts
uv run --with python-docx --with pyyaml python3 $S/fill_application_form.py 草稿/申请表信息.yaml -o 正式资料/
uv run --with python-docx python3 $S/build_manual_docx.py 草稿/操作手册.md --name "<全称>" --version V1.0 --owner "<权利人>" -o 正式资料/
uv run --with python-docx python3 $S/build_code_docx.py --root <项目根> --files 草稿/代码文件顺序.txt --name "<全称>" --version V1.0 -o 正式资料/ [--strip-vue-style]
```
`--name/--version/--owner` 必须直接取自 yaml，不要手敲。看 `build_code_docx.py` 打印的首页起始/末页结束，确认是入口和完整收尾；否则调整顺序重跑。

### 第 6 步：自检

```bash
uv run --with python-docx --with pyyaml python3 $S/check_consistency.py 正式资料/ --yaml 草稿/申请表信息.yaml
```
全部通过后，告知用户人工核对项：
1. 用 Word 打开手册 → 全选 → F9 更新目录 → 保存
2. 翻看每张截图：含标题栏、系统名与申报名一致、无多余马赛克
3. 权利人、证件号、完成日期与实名认证一致
4. 然后 `bash $S/docx_to_pdf.sh 正式资料/`，PDF 控制在 10–50MB

## 脚本一览（`scripts/`）

| 脚本 | 作用 |
|---|---|
| `collect_sources.py` | 扫描项目，输出候选文件表 + 真实源程序量 + 语言汇总 |
| `build_code_docx.py` | 有序文件清单 → 50 行/页、前30后30、页眉页码、末页标注的代码 Word + 页索引；`--strip-vue-style` 去掉 .vue 样式块，避免核心页被 CSS 占满 |
| `build_manual_docx.py` | 手册 Markdown → 封面/目录/页眉页码/插图图注的 Word |
| `fill_application_form.py` | yaml → 填公司申请表模版 docx + 申请表信息.txt，含字数校验 |
| `check_consistency.py` | 三份材料一致性与硬指标自检 |
| `docx_to_pdf.sh` | LibreOffice 批量转 PDF 并报大小 |
| `docx_common.py` | 公共：页眉页码域、字体、网格、分页 |

依赖：`uv`、`python-docx`、`pyyaml`、LibreOffice（`soffice`，转 PDF 用）。

## 不做什么

- 不保证通过审查；不伪造权属、完成日期、发表状态
- 不把开源项目改头换面当原创核心
- 不代替用户在版权中心系统填报、签章、上传（官方：中国版权保护中心著作权登记系统，010-61090099）
